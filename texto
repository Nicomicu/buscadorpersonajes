
  const consultarPersonaje = async (personaje) => {
    try {
      const { name, films, height } = personaje
      const appId = import.meta.env.VITE_API_KEY

      const url = `https://swapi.dev/api/people`
      const { data } = await axios(url)
      console.log(data)
    } catch (error) {
      console.log(error)
    }
  }